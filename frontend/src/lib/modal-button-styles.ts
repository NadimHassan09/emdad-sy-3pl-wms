import { FILTER_RESET_BUTTON_CLASS } from '@ds';

/** Modal cancel — same red pill as Reset filters. */
export const MODAL_CANCEL_BUTTON_CLASS = FILTER_RESET_BUTTON_CLASS;

/** @deprecated Use Button `variant="brand"` — kept for any extra className overrides. */
export const MODAL_PRIMARY_BUTTON_CLASS = '';
