# WebSocket Connection header mapping + backend upstream (HTTP block).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream emdad_wms_backend {
    server 127.0.0.1:3000;
    keepalive 32;
}
