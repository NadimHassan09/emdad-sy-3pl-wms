# Client portal — production build on client.emdadsy.com (same-origin /api/client).
# Must be an ABSOLUTE URL: socketHttpOrigin() parses it with `new URL(...)`
# to derive the Socket.IO origin (`/realtime`).
VITE_API_URL=https://client.emdadsy.com/api/client
