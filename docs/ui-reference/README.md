# Client Portal v2 — approved HTML (source of truth)

File: `client-portal-v2.html`

This HTML is the approved visual design for the Client Portal. React under
`client-frontend/` must match it faithfully (layout, spacing, chrome, pages).

Do not treat the previous Client Portal UI as a design reference.
